# Josh's Homebrew Tap
Homebrew tap for shipping my CLI tools

## Install

```bash
brew tap jdblackstar/tap
```

## Install Packages

```bash
# formula
brew install jdblackstar/tap/<name>

# cask
brew install --cask jdblackstar/tap/<name>
```

## Packages

### Formulae

- `relay` — Sync skills, commands, and rules between agent tools.

## Update / Uninstall

```bash
brew update
brew upgrade

brew uninstall <formula>
brew uninstall --cask <cask>